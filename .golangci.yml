version: "2"

run:
  timeout: 5m
  go: "1.23"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - misspell
    - gosec
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
